<h1>Burgers!</h1>

<h2>Smash these Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#unless smashed}}
      {{> burgers/burger-block smashed=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Completely smashed Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#if smashed}}
      {{> burgers/burger-block smashed=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burger">Burger Name:</label>
    <input type="text" id="burger" name="name">
  </div>

  <div class="form-group">
    <label for="smashed">smashed?</label><br>
    <input type="radio" name="smashed" value="1" checked> Smashed!<br>
    <input type="radio" name="smashed" value="0"> UnSmashed Burgers
  </div>

  <button type="submit">Add Burger</button>
</form>
